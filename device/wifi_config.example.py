# Template for device/wifi_config.py (gitignored -- contains real secrets,
# never committed). Copy this file to device/wifi_config.py and fill in
# real networks. Order matters: earlier entries are tried first.
NETWORKS = [
    {"ssid": "some-network", "password": "some-password"},
    {"ssid": "another-network", "password": "another-password"},
]
