# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Downloaded MicroPython firmware images
firmware/*.bin

# Secrets -- never commit the real countdown data JSON (which may embed
# Basic Auth credentials in its meta.url, and real Wi-Fi passwords in its
# meta.wifi_networks)
device/countdown_data.json

# Machine-specific serial port (see README.md "Set your port once")
port.txt
*~

# Local record of the last successful `make install-python` (see Makefile)
.stamp-python
